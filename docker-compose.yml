services:
  web:
    image: nginx:1
    ports:
      - "8080:8080"
    environment:
      LOG_LEVEL: info
      WORKER_THREADS: "4"
    volumes:
      - ./html:/usr/share/nginx/html:ro
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: changeme
